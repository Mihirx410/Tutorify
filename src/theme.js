const theme = {
    modes: {
      light: {
        lightGray: '#F5F5F5', // Background
        darkGrayBlack: '#1A1A1A', // Main Heading
        mediumGray: '#666666', // Subheading
        orange: '#FF6200', // Button
        white: '#FFFFFF', // Cards
        darkGray: '#333333', // Stats Text
        lightOrange: '#FFDAB9', // Image Overlay
      },
      dark: {
        lightGray: '#1A1A1A', // Background (dark background)
        darkGrayBlack: '#E0E0E0', // Main Heading (light text for contrast)
        mediumGray: '#A0A0A0', // Subheading (lighter gray for contrast)
        orange: '#FF6200', // Button (keep the same for consistency)
        white: '#2A2A2A', // Cards (darker background for cards)
        darkGray: '#B0B0B0', // Stats Text (light gray for contrast)
        lightOrange: '#D4A276', // Image Overlay (darker shade of orange)
      },
    },
    fonts: {
      primary: 'Poppins, sans-serif',
      secondary: 'Roboto, sans-serif',
    },
  };
  
  export default theme;